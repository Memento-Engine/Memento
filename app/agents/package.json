{
  "scripts": {
    "dev:agent": "cross-env MEMENTO_DEV=true tsx watch --include src/**/*.ts src/server.ts",
    "build:bundle": "esbuild src/bootstrap.ts --bundle --platform=node --target=node20 --format=cjs --define:__DEV__=false --outfile=dist/memento-agents.cjs",
    "build:exe": "npm run build:bundle && npx @yao-pkg/pkg dist/memento-agents.cjs --targets node20-win-x64 --output dist/memento-agents.exe",
    "typecheck": "tsc --noEmit --watch",
    "dev": "concurrently \"npm run dev:agent\" \"npm run typecheck\"",
    "latency:tail": "powershell -NoProfile -Command \"New-Item -ItemType Directory -Force logs | Out-Null; if (!(Test-Path logs/latency.log)) { New-Item -ItemType File logs/latency.log | Out-Null }; Get-Content logs/latency.log -Wait\"",
    "latency:clear": "powershell -NoProfile -Command \"New-Item -ItemType Directory -Force logs | Out-Null; Clear-Content logs/latency.log -ErrorAction SilentlyContinue\""
  },
  "dependencies": {
    "@langchain/core": "^1.1.30",
    "@langchain/langgraph": "^1.2.0",
    "@sentry/node": "^10.22.0",
    "@types/chokidar": "^1.7.5",
    "axios": "^1.13.6",
    "chokidar": "^5.0.0",
    "cors": "^2.8.6",
    "env-paths": "^4.0.0",
    "express": "^5.2.1",
    "lodash": "^4.17.23",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0",
    "pino-pretty": "^13.1.3",
    "uuid": "^9.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/lodash": "^4.17.24",
    "@types/node": "^25.3.3",
    "@types/uuid": "^9.0.1",
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3",
    "esbuild": "^0.27.4",
    "@yao-pkg/pkg": "^6.7.1",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3"
  }
}
